# Borderless — 교회 청년부 수련회 미션 앱

## 상태

참가자 플로우(팀/조 선택 → 지도 → 사진 제출 → AI 판정 → 미션 공개 → 영상 업로드)와 임원 플로우(비밀 경로 setup CRUD, 도움 요청, 지급 확정, 대시보드, 영상 모아보기) 모두 구현 완료, Vercel에 배포됨 (2026-07-15). 이후 실기기 테스트를 반복하며 발견된 버그들(카메라 강제 연동, 사진 크롭, 지도 마커 상태 미갱신, 패널 상태 누수, 확대 오버레이가 실기기에서 안 닫힘 등)을 계속 수정했고, 어드민 쪽에 사진/미션/재료/판정질문 사후 수정, 제출 초기화, 팀 선택 잠금 기능도 추가함 (2026-07-19). 2026-07-20에는 어드민 "새 포인트 추가" 폼에 지도 클릭 좌표 지정(주소 검색 시 자동 확대·이동)을 넣었고, 미션 인라인 수정, 포인트를 삭제 없이 숨길 수 있는 활성/비활성 토글, 나침반 권한을 기억해 다음 방문부터 재요청 안 하게 하는 것, 새 포인트 등록 후 폼 자동 초기화를 추가했다. 2026-07-21에는 Ingredient 모델에서 실제로 안 쓰이던 category/variant/isBase 필드를 정리하고 미션에 퀴즈(PUZZLE) 타입을 추가했으며(영상 업로드 대신 정답 텍스트 제출로 완료), 도움 요청에 조원 이름과 메시지가 함께 남게 했고, 참가자가 매번 입력하던 조원 이름을 어드민이 미리 등록해두는 방식으로 전환했다(팀 선택 화면에서 이름 입력 제거). 지도 마커에 선택 상태·비활성(차례 아님/마감) 표시를 다듬었고, 미션 수행 영상 안내를 "10~20초"에서 "30초 이상"으로 조정했다. 2026-07-22에는 어드민 화면을 **설정**(지역/미션/재료/방문포인트)과 **팀**(팀·조 이름/조원, 그룹 선택 잠금, 그룹별 지역 방문 순서, 그룹별 진행, 도움 요청, 포인트별 현황·지급확정) 2탭으로 재편했고(기존 도움요청·대시보드 페이지 통합), 지도에 "같은 팀 다른 조가 이 지역을 이미 통과했다"는 안내를 추가했다. PC 화면에서는 지도(왼쪽)·상세 패널(오른쪽) 6:4 비율 2단 레이아웃을 별도로 지원한다(모바일은 기존 그대로 세로 스택). 차례가 아닌 지역/이미 마감된 포인트를 선택했을 때는 평범한 안내 문장 대신 PASS 카드와 같은 골격의 LOCKED/CLOSED 카드로 강조 표시한다. 2026-07-27에는 다른 임원에게 받은 자료를 반영해 재료 마스터 목록을 리허설용 11종에서 실제 행사에 쓸 16종(라면사리/쫄면사리/납작당면/우동사리, 치즈떡/고구마떡/쌀떡/밀떡, 사각어묵/꼬치어묵/봉어묵/모둠어묵, 모짜렐라(치즈)/비엔나/만두/김말이)으로 완전 교체했고, 그룹별 지역 방문 순서도 더미(전 그룹 a→b→c→d)에서 실제 확정 순서(1조: d→b→a→c, 2조: c→a→b→d, 전 팀 공통)로 프로덕션 DB와 `prisma/seed.ts`에 반영했다. 같은 날, 포인트당 미션을 1개에서 2개(슬롯1/슬롯2)로 확장했다 — 같은 포인트에 먼저 AI 판정을 통과한 조는 슬롯1, 두 번째로 통과한 조는 슬롯2 미션을 받는다(4가지 미션 타입 전부 적용). 각 조가 올린 영상을 나중에 이어붙여 하나의 완성된 찬양/말씀 낭독을 만들려는 목적이라, 같은 포인트의 두 조가 겹치는 내용을 찍지 않게 하기 위함. `Location.missionId` 단일 관계를 `mission1Id`/`mission2Id`로 나눴고(마이그레이션으로 기존 데이터는 슬롯1로 이관), `Submission.missionSlot`에 어느 슬롯을 획득했는지(캡 원자적 증가 시점의 `claimedCount` 값) 기록한다. 같은 날, 참가자 지도에 숙소(펜션) 마커를 추가했다 — DB에 등록되는 Location이 아니라 주소를 지오코딩해서 항상 표시되는 참고용 아이콘이며(지역당 4곳 카운팅 로직과 무관), 클릭하면 우리 조의 지역별 진행 현황·모은 재료·같은 팀 다른 조 현황을 보여주는 패널이 뜬다. 퀴즈(PUZZLE) 미션 타입은 실제로 쓸 일이 없어져 완전히 제거하고, 정답 텍스트 제출/채점 기능(Mission.answer, Submission.answerText/answerCorrect, 관련 API 라우트)까지 다 정리한 뒤, 참가자가 "나는 ~입니다" 형식으로 자기만의 신앙 고백을 짓고 영상으로 촬영하는 **고백(CONFESSION)** 타입으로 교체했다(다른 세 타입처럼 영상 업로드로 완료, 정답 판정 없음 — 슬롯1/2도 같은 지시문을 써도 무방한데, 참가자마다 실제로 짓는 문장이 달라 자연히 안 겹침). 어드민 쪽엔 방문포인트 수정 시 위치(위도/경도)도 지도 클릭·주소 검색·GPS로 다시 잡을 수 있게 했고(현장 답사 후 세부 위치 조정 대비), 판정질문 입력창을 평소엔 짧게 보이다가 포커스하면 커지게 했다. 2026-07-28에는 지도를 Kakao Maps에서 Naver Maps(NCP 신상품 'Maps')로 전면 교체했다 — 답사 예정 지역(강화군 하도리)의 카카오 로드뷰가 2020년에 멈춰 있어 최근 생긴 도로가 안 보이는 문제 때문(네이버 로드뷰는 2024년 기준). 참가자 지도(`NaverMap.tsx`)와 어드민 좌표 찍기(`LocationMapPicker.tsx`) 모두 이식했고, Client Secret이 필요한 Geocoding/Reverse Geocoding은 브라우저에서 직접 호출할 수 없어 서버 API 라우트(`/api/geocode`, `/api/reverse-geocode`)로 옮겼다. Vercel 배포본에서도 확인 완료. 다만 네이버 Web Dynamic Map의 기본 지도 스타일에는 버스정류장 등 대중교통 POI 아이콘이 없다는 걸 확인했고(공식 레이어 종류엔 자전거도로/교통정보/거리뷰영역/지적편집도뿐), 이건 API 자체의 제약이라 코드로 해결하지 않고 그대로 두기로 함. 남은 건 답사(현장 답사 후 실제 16개 포인트 등록·프롬프트 조정, 그리고 이미 등록된 리허설 포인트들에 슬롯2 미션 채워넣기) — 이건 이 리포지토리 밖에서 정해지거나 admin에서 수동으로 진행해야 하는 부분. 2026-08-04에는 어드민에 **갤러리** 탭을 추가했다 — 처음엔 팀 탭과 거의 같은 구조(포인트별로 묶고 실패 사진 포함)로 만들었다가 "팀 탭이랑 다를 게 없다"는 지적을 받고, 완성 영상(통과+영상 있는 것만, 나중에 이어붙이기 편하도록 포인트가 아니라 미션 타입별로 그룹)과 실패 사진 모아보기(포인트 구분 없는 단일 그리드, 회고용) 2섹션으로 재설계했다. 같은 날, 2인1조로 지역 간 이동하는 동안의 어색함을 덜기 위한 **대화 주제 카드** 기능을 추가했다 — 지도 화면 상시 버튼을 누르면 LocationPanel/PensionPanel과 같은 패널 슬롯에 주제(주질문+부질문) 카드가 뜨고, 기기 localStorage에 뽑은 이력을 남겨 100문항을 다 소진할 때까지 안 겹치게 한다. 문항은 사용자가 준비한 카드게임(GENTHLX) 100세트를 그대로 썼고, 교회 청년부 자리에 부적절한 문항(테러 소재, 허무주의 소재) 2개는 검수해서 제외했다. 또한 4지역 미션을 모두 완료(영상 업로드까지)하면 지도 화면 상단에 "숙소로 돌아가세요" 배너가 상시 뜨도록 했다(기존 AI 판정 중단 배너와 시각적으로 구분되는 색 사용, `region-progress.ts`의 완료 판정 재사용). 2026-08-08에는 소속 선택 화면과 지도 화면에 "그을린 양피지" 톤 배경 실험을 진행했다 — 기존 밝은 종이/잉크/오렌지 톤이 "초안처럼 밋밋하다"는 피드백에서 시작해, 처음엔 도트 그리드+그림자 정도로 살짝 손봤다가("모눈종이로 바꾼 느낌"이라는 재반려), GPT로 따로 실험해본 트레저맵 톤(세피아, 하드코딩 색)을 참고해 같은 방향을 디자인 토큰 위에서 다시 만들었다. 최종적으로 `ParchmentBurn.tsx`(feTurbulence로 흔든 두 겹 그을린 테두리, `outerWidth`/`innerWidth`/`insetPercent` prop으로 화면마다 두께·위치 조절), `ParchmentStains.tsx`(CSS radial-gradient 타원 대신 feTurbulence 노이즈 자체를 얼룩 모양으로 써서 "도장 찍은 것 같다"는 문제를 해결 — `idPrefix`로 여러 인스턴스의 SVG filter id 충돌을 피하고 `intensity`로 패널마다 진하기를 낮춤), `MapDecoration.tsx`(점선 루트 + 손그림 느낌 브러시 X마크, 팀 컬러 4색을 그대로 쓰면 "장난감 지도 같다"는 반려를 받아 낡은 잉크 톤 하나로 통일)로 정리했다. X마크는 처음에 뾰족한 양끝의 도형이라 "수리검 같다"는 지적을 받아 사용자가 준 CSS 레퍼런스(clip-path 다각형 붓터치)를 SVG 좌표로 옮겨 뭉툭한 붓자국 모양으로 바꿨고, 잉크 튀김 점은 개수를 몇 차례 조정한 끝에 "튄 자국은 흔치 않다"는 이유로 마크당 0~1개로 정리했다. 이 톤을 지도 화면(`MapScreen.tsx`)까지 넓히면서, 헤더가 화면 가장자리에 바짝 붙어 테두리와 겹치는 문제를 겪었고 — 안쪽 padding을 늘리는 대신 `insetPercent`로 테두리 자체를 화면 끝 쪽으로 밀어내는 방식으로 해결했다. 지도 화면의 하위 패널(`LocationPanel`/`PensionPanel`/`ConversationTopicPanel`, 그리고 포인트 미선택 시 빈 안내 패널)에도 `parchment-panel` 클래스(배경보다 한 톤 밝은 베이스 + 같은 섬유선 결)와 옅은 `ParchmentStains`를 얹어 통일했다. 이 과정에서 안 쓰던 인벤토리 화면(`/inventory`)을 발견해 완전히 삭제했다 — 지도 화면의 링크만 없었을 뿐 라우트는 남아있었음. 임원(어드민) 화면은 사용자만 보는 도구라 이번 톤 확장 대상에서 제외했다. 진행 상황·이슈는 [notes/](notes/) 디렉터리에 날짜별로 기록. 상세 기획은 [Borderless_클로드코드_개발기획서.md](Borderless_클로드코드_개발기획서.md)가 원본(source of truth)이니, 세부 사항은 항상 그 문서를 먼저 확인할 것 — 아래는 빠르게 감을 잡기 위한 요약일 뿐 이 파일을 근거로 세부 구현 결정을 내리지 말 것.

이 프로젝트는 Next.js 템플릿이 생성한 `AGENTS.md`도 포함하고 있음 — 이 Next.js 버전이 훈련 데이터와 다를 수 있다는 경고이니 코드 작성 전 참고할 것.

## 한 줄 요약

참가자(8그룹)가 지도로 안내된 야외 지점에서 사진을 찍어 올리면 AI(VQA 방식)가 기준 사진과 비교해 통과 여부를 판정하고, 통과 시 미션(말씀/찬양/기도/고백)이 공개되며 재료를 모아가는 모바일 웹앱.

## 핵심 구조 (변경 시 기획서부터 갱신)

- 4팀(A~D) × 팀당 2조 = 8그룹. 4지역(a~d) × 지역당 4포인트 = 총 16포인트.
- 그룹은 지역마다 포인트 1곳씩, 정확히 4곳만 방문 (그 이상도 이하도 없음).
- **그룹별로 지역 방문 순서가 강제됨** (2026-07-27 확정: 팀별 1조는 d→b→a→c, 2조는 c→a→b→d, 전 팀 공통 — DB `GroupRegionOrder`와 admin "팀" 탭에서 조별로 다시 바꿀 수 있음). 지도 마커 자체는 잠그지 않지만, 차례가 아닌 지역의 장소 상세(기준 사진/업로드)는 숨기고 대신 LOCKED 카드로 안내(같은 지역이라도 미션 영상 업로드가 안 끝났으면 CLOSED가 아니라 별도 안내). 이미 마감된 포인트는 CLOSED 카드. 서버에서도 이중 검증.
- 포인트당 선착순 2그룹 캡. **캡 확인은 AI 판정 전에 먼저 실행되고, 참가자에게는 방문 전 절대 노출 안 됨** — 사진 업로드 시점에 처음 "마감" 여부를 알게 됨. AI 통과 즉시 서버가 캡을 원자적으로 차감(동시성 처리 필수). 마감 확인 정보는 **팀 단위**로 공개(같은 팀 다른 조에게도 보임, 타 팀은 비공개).
- **포인트마다 미션이 2개(슬롯1/슬롯2)** (2026-07-27 확정). 그 포인트에 먼저 AI 판정 통과한 조는 슬롯1, 두 번째로 통과한 조는 슬롯2 미션을 받는다(`Submission.missionSlot`, 캡 원자적 증가와 같은 원자적 UPDATE에서 결정). 각 조 영상을 이어붙여 하나의 완성된 찬양/말씀을 만들 목적이라, 같은 포인트의 두 조가 같은 내용을 찍지 않게 하기 위함 — 4가지 미션 타입(WORD/PRAISE/PRAYER/CONFESSION) 전부 적용.
- 재료의 물리적 지급은 오프라인(임원이 현장에서 직접). 관리자 화면의 "지급 확정"은 캡을 가르는 게이트가 아니라 전달 기록용. 처리자가 누구인지는 기록하지 않고 완료 여부만 추적(임원이 소수라 불필요).
- 재료 교환 기능은 이번 버전 범위에서 완전히 제외 (오프라인으로 처리).
- **지역 "완료"는 AI 판정 통과만으론 안 되고, 미션 완료(영상 업로드)까지 끝나야 확정된다** (2026-07-15 확정; 2026-07-21 추가했던 퀴즈(PUZZLE) 타입의 정답 텍스트 제출 완료 경로는 2026-07-27 퀴즈 타입 제거와 함께 정리됨). 그 전까지는 상단 진행 표시·"다음 목적지" 안내가 그 지역에 그대로 머문다. 같은 지역의 다른 포인트에서 이미 통과했다면(미션 완료 전이라도) 재시도는 서버에서 차단되고 "그 포인트로 돌아가라"는 안내만 표시.
- AI 판정은 CLIP/CNN 임베딩이 아니라 **VQA(자연어 질문) 방식**, **OpenAI 비전 지원 모델**(GPT-4o 계열, 정확한 모델명은 env var로 설정) 사용 (2026-07-14 변경: 원래 Claude 비전 모델이었으나 결제 방식 이유로 OpenAI로 전환).
- 지도는 Naver Cloud Platform 'Maps'(Web Dynamic Map + Geocoding/Reverse Geocoding, 무료 쿼터로 충분) + 브라우저 GPS로 내 위치 표시. GPS는 내비게이션 편의용이며 판정에는 쓰지 않음. (2026-07-28: 원래 Kakao Maps였으나, 답사 지역의 카카오 로드뷰가 2020년에 멈춰 있어 최근 도로가 안 보이는 문제로 네이버 Maps 신상품으로 전환 — Geocoding은 Client Secret이 필요해 서버 API 라우트(`/api/geocode`, `/api/reverse-geocode`)를 거침.)
- 장소 등록은 현장(답사 당일) 빠른 입력이 기본 전제 — "내 위치 사용" GPS 버튼 + 폰 카메라 촬영이 메인 흐름, judgePrompt는 공통 기본 템플릿에서 시작해 나중에 원격으로 다듬음.
- 재시도 횟수 제한 없음. "임원 도움 요청" 버튼 상시 노출 — 사전 답사가 사실상 불가능해 AI 판정을 미리 검증할 기회가 거의 없으므로 안전망이 아니라 주력 백업 수단.
- 임원 인증 없음 — 추측 어려운 비밀 URL 경로로만 관리자 화면 접근 (로그인/비밀번호 없음).
- 기술 스택: Next.js(App Router, TS) + Prisma + Supabase(Postgres+Storage, 로컬은 SQLite로 시작 가능) + Naver Maps(NCP Maps) + OpenAI vision API + Vercel 배포.

## 행사 일정 및 장소

- 수련회: 2026-08-16 ~ 2026-08-18
- 숙소(기준점): 인천 강화군 송해면 오류내길99번길 40-7 — 미션 장소 4곳은 이 숙소에서 약 30분 거리 반경 내에서 선정 예정
- 답사는 행사 전날 다른 프로그램 진행 중 짬을 내서 이뤄질 가능성이 높음 (재답사 기회 보장 안 됨) — 그 전에 사용자 주변의 실제 장소로 전체 플로우를 미리 리허설해둘 것

## 아직 미확정 (기획서 8절 참고)

- 판정 임계값/프롬프트 최종본

## 디자인

방향 재확정 (2026-07-15, notes/26.07.15.md 참고). 실제 지도를 화면에 넣어보고 나서 "장난감 같다"는 피드백을 거쳐, 베이스 톤은 유지하되 표현 방식을 바꿨다.

- **베이스**: 밝은 배경 + 짙은 잉크 텍스트 + 오렌지 액센트라는 큰 방향은 그대로, 다만 카키톤 하나가 아니라 **투톤(카키 페이지 + 흰색 계기 카드)** — 헤더/여백은 카키(`--color-paper` `#F5F3EC`), 지도+패널을 감싸는 프레임 내부는 거의 흰색(`--color-paper-panel` `#FFFFFF`)에 가깝게. 텍스트 `--color-ink` `#1C211D`, 액센트 `--color-accent` `#E1591C`. 팀(A~D) 컬러는 배경과 구분되는 별도 4색 사용.
- **테두리/모서리는 두꺼운 flat 잉크선 대신 헤어라인(1px) + 미세한 그림자**로. 알약형(rounded-full) 배지·버튼도 각진 카드(6~8px 라운드)로 교체 — 원인이 색이 아니라 이 두꺼운 테두리와 둥근 알약형이었음.
- **미션 통과 리빌 화면**: 원형 잉크 스탬프 대신 보딩패스/permit 카드 톤의 각진 카드(PASS + 코드 + 미션 내용)로 변경, 뜨는 순간 오렌지 빛이 1초 정도 한 번 번지는 정도로만 임팩트를 준다 (반복 재생 안 되게 주의).
- **"Borderless" 모티프**: 헤더에 지역 진행 스테퍼(a-b-c-d, 완료/현재/예정 상태 표시) + 현재 위치를 나타내는 점을 두고, 그 점에서 짧은 연결선이 지도 프레임 위 목적지 배지로 이어지게 해서 "경계를 넘어 계속 나아간다"는 걸 장식이 아니라 정보 구조로 표현한다.
- 상시 노출되는 "임원 도움 요청" 버튼은 어떤 화면에서도 눈에 띄게 유지 — 단, 다른 UI(특히 패널 텍스트)를 가리지 않도록 여유 공간을 확보해야 함.
- **차단/대기 안내(LOCKED/CLOSED)**: "차례가 아님"/"마감" 같은 상태는 평문으로 두면 잘 안 읽혀서, PASS 카드와 같은 카드 골격(헤어라인 accent 테두리 + 점선 헤더 구분선)에 영문 스탬프 라벨(LOCKED/CLOSED)을 얹어 표시한다. 이모지·흔들림 애니메이션·진동은 시도했다가 "AI가 만든 티가 난다"는 피드백으로 뺐고, 카드가 한 번 살짝 떠오르는 정도의 절제된 모션만 남겼다. 문구도 "~해요" 대신 표지판처럼 짧게 끊는 톤("A지역부터 먼저 완료").
- **PC 전용 레이아웃**: 참가자 지도 화면은 `lg:` 이상에서만 지도(좌, ~60%)·상세 패널(우, ~40%) 2단으로 나뉘고 그 아래 폭에서는 기존 모바일 세로 스택 그대로다. 네이버 지도는 컨테이너 폭이 바뀌어도 스스로 다시 그리지 않아 `ResizeObserver` + `autoResize()`로 보정한다.
- 비교 검토했던 다른 방향(워십 슬라이드형 다크 톤, 여권/스탬프북형 크래프트지, 팀 스코어보드형 경쟁 톤, 다크 나이트옵스)은 기각 — 다크 배경은 실외 가독성이 떨어지고, 스코어보드 톤은 말씀/기도 리빌과 어울리지 않는다고 판단.
- 정확한 토큰 값은 `src/app/globals.css`의 `@theme` 블록이 최신 소스.
- **그을린 양피지 실험** (2026-08-08, notes/26.08.08.md 참고): 소속 선택·지도 화면에 한해 위 베이스 톤과 별개로 세피아 양피지 배경(`--parchment-*` 로컬 변수, 전역 `--color-*` 토큰과 분리)을 시도 중 — `ParchmentBurn`(그을린 테두리) + `ParchmentStains`(feTurbulence 노이즈 얼룩) + `MapDecoration`(점선 루트·붓터치 X마크). 다른 화면(PASS 카드, 어드민 등)의 밝은 종이 톤과는 아직 안 섞여 있고, 확대 여부는 미정.
